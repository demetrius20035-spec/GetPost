# GUI-фреймворк. Приложение поддерживает и PySide6, и PyQt6 —
# достаточно установить любой из них. По умолчанию используется PySide6.
PySide6>=6.5
# Альтернатива (раскомментируйте, если предпочитаете PyQt6, и можно убрать PySide6):
# PyQt6>=6.5

# HTTP-клиент.
requests>=2.25
